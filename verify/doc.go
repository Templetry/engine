// Package verify runs the manifest's verify command inside a Docker
// container (verify: {image, run}). The engine knows "docker run" and
// nothing else about toolchains (ADR-0004).
package verify
