// Package ops defines the engine's operation types as data
// (CopyFile, RenamePath, FilterDirectives, ApplyPatch, WriteAnswers).
// Operations carry no behavior; the renderer executes them.
package ops
